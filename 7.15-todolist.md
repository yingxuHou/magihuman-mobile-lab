# 7.15 TodoList：MagiHuman Mobile Lab

## 项目信息

项目名：MagiHuman Mobile Lab

GitHub 仓库名建议：`magihuman-mobile-lab`

项目介绍：

本项目用于复现和评估开源数字人生成模型 daVinci-MagiHuman。目标不是只跑通官方 demo，而是用可记录、可复查的数据判断它是否能被集成进手机 App。项目会依次完成官方资料核验、环境搭建、最小推理复现、性能测试、显存/耗时/模型体积记录、服务端接口验证，以及移动端部署可行性分析。

当前结论状态：

- [x] 根据文章和官方指标形成初步假设：完整模型大概率不适合手机端侧推理。
- [ ] 该结论还没有本地或云 GPU 实测数据支撑，不能作为最终结论。
- [ ] 最终结论必须基于以下实测数据：模型体积、峰值显存、推理耗时、依赖复杂度、输出质量、服务端封装成本、移动端接入成本。

## 判断标准

端侧 App 可行的定义：

- [ ] 模型和依赖能在 iOS / Android 本机运行。
- [ ] 不依赖远程 GPU 服务。
- [ ] 模型体积、内存占用、耗时、电量消耗在手机上可接受。
- [ ] 输出质量达到可演示标准。

云端 App 可行的定义：

- [ ] 模型可以在 GPU 服务器稳定推理。
- [ ] 手机端通过 API 上传输入并获取结果。
- [ ] 单任务耗时、排队、成本、失败重试机制可接受。
- [ ] 生成结果能被手机端顺畅播放、保存和分享。

最终结论必须写成三选一：

- [ ] A. 可以端侧部署。
- [ ] B. 不能端侧部署，但可以做“手机 App + 云端 GPU 推理”。
- [ ] C. 当前复现成本或效果不达标，暂不适合做产品化 App。

## 0. 官方资料核验

目标：确认复现依据来自官方资料，不只依赖转述文章。

- [x] 已阅读 `article.md`。
- [ ] 核验论文：https://arxiv.org/abs/2603.21986
- [ ] 核验代码：https://github.com/GAIR-NLP/daVinci-MagiHuman
- [ ] 核验模型：https://huggingface.co/GAIR/daVinci-MagiHuman
- [ ] 核验在线试用：https://huggingface.co/spaces/SII-GAIR/daVinci-MagiHuman
- [ ] 记录官方仓库 commit hash。
- [ ] 记录模型仓库最后更新时间。
- [ ] 记录 license 和商用限制。
- [ ] 记录官方推荐的 Python、CUDA、PyTorch、MagiCompiler、flash-attention 版本。
- [ ] 找到官方推理入口脚本和示例命令。
- [ ] 找到官方 demo 输入素材格式：图片、文本、音频、视频尺寸、输出目录。

资料核验记录表：

| 项目 | 结果 | 备注 |
| --- | --- | --- |
| 论文标题 | 待记录 |  |
| 代码仓库 commit | 待记录 |  |
| 模型 license | 待记录 |  |
| 权重文件总大小 | 待记录 |  |
| 官方最小推理命令 | 待记录 |  |
| 官方推荐硬件 | 待记录 |  |
| 官方 H100 速度指标 | 待记录 |  |

通过标准：

- [ ] 能明确知道要下载哪些权重。
- [ ] 能明确知道运行哪个推理脚本。
- [ ] 能明确知道最小 demo 需要哪些输入文件。

## 1. 本机硬件与工具检查

目标：先判断当前机器是否值得直接复现，避免在不合适的环境里浪费时间。

执行命令：

```powershell
nvidia-smi
nvcc --version
git --version
git lfs version
conda --version
docker --version
ffmpeg -version
python --version
```

硬件记录表：

| 项目 | 结果 |
| --- | --- |
| 操作系统 | 待记录 |
| GPU 型号 | 待记录 |
| GPU 数量 | 待记录 |
| 单卡显存 | 待记录 |
| NVIDIA Driver | 待记录 |
| CUDA Runtime | 待记录 |
| CUDA Toolkit | 待记录 |
| Docker 是否可用 | 待记录 |
| Conda 是否可用 | 待记录 |
| 磁盘可用空间 | 待记录 |

决策：

- [ ] 如果没有 NVIDIA GPU，直接转云 GPU。
- [ ] 如果显存明显不足，仍可做代码阅读和接口设计，但不做本地完整推理。
- [ ] 如果 Docker 可用，优先走 Docker 路线。
- [ ] 如果 Docker 不可用，再尝试 Conda 本地路线。

## 2. 复现路线选择

首选路线：Docker

原因：

- [ ] 官方文章给出了 `sandai/magi-compiler:latest` Docker 镜像。
- [ ] MagiCompiler、CUDA、flash-attention、PyTorch 版本容易冲突，Docker 更容易复现。
- [ ] 后续服务端部署也更适合容器化。

备选路线：Conda

使用条件：

- [ ] Docker 不可用。
- [ ] 需要调试源码。
- [ ] 当前机器 CUDA / PyTorch / 编译工具链版本可控。

Docker 路线待执行：

```powershell
docker pull sandai/magi-compiler:latest
docker images | Select-String magi-compiler
```

Conda 路线待执行：

```powershell
conda create -n davinci-magihuman python=3.12
conda activate davinci-magihuman
conda install ffmpeg
```

通过标准：

- [ ] 能进入可用 Python 环境。
- [ ] 能 import torch。
- [ ] 能识别 GPU。
- [ ] 能编译或加载 MagiCompiler 相关依赖。

## 3. 代码准备

目标：保持官方代码独立，便于后续同步和记录改动。

执行步骤：

- [ ] 在本仓库下创建 `third_party/`。
- [ ] 克隆官方代码到 `third_party/daVinci-MagiHuman`。
- [ ] 克隆 MagiCompiler 到 `third_party/MagiCompiler`，或者使用 Docker 镜像内置版本。
- [ ] 记录两个仓库的 commit hash。
- [ ] 不直接修改官方源码；如必须修改，先用 patch 文件记录。

命令模板：

```powershell
New-Item -ItemType Directory -Force third_party
git clone https://github.com/GAIR-NLP/daVinci-MagiHuman third_party/daVinci-MagiHuman
git clone https://github.com/SandAI-org/MagiCompiler third_party/MagiCompiler
git -C third_party/daVinci-MagiHuman rev-parse HEAD
git -C third_party/MagiCompiler rev-parse HEAD
```

记录表：

| 仓库 | commit | 是否修改 | 备注 |
| --- | --- | --- | --- |
| daVinci-MagiHuman | 待记录 | 否 |  |
| MagiCompiler | 待记录 | 否 |  |

## 4. 模型权重准备

目标：下载并记录所有必须权重，确认模型体积是否已经超出手机 App 端侧可接受范围。

执行步骤：

- [ ] 安装 Hugging Face CLI。
- [ ] 登录 Hugging Face，确认是否需要申请权限。
- [ ] 下载 `GAIR/daVinci-MagiHuman` 到本地模型目录。
- [ ] 记录权重总大小。
- [ ] 记录单个最大文件大小。
- [ ] 记录是否包含基础模型、蒸馏模型、超分模型、VAE、tokenizer、配置文件。

命令模板：

```powershell
pip install -U huggingface_hub
huggingface-cli login
huggingface-cli download GAIR/daVinci-MagiHuman --local-dir models/daVinci-MagiHuman
Get-ChildItem models/daVinci-MagiHuman -Recurse | Measure-Object -Property Length -Sum
Get-ChildItem models/daVinci-MagiHuman -Recurse | Sort-Object Length -Descending | Select-Object -First 20 FullName,Length
```

模型记录表：

| 项目 | 结果 |
| --- | --- |
| 权重总大小 | 待记录 |
| 最大单文件 | 待记录 |
| 文件数量 | 待记录 |
| 是否需要 Git LFS | 待记录 |
| 是否需要额外外部权重 | 待记录 |
| 是否可接受放进 App 包 | 待记录 |

判断规则：

- [ ] 如果权重达到几十 GB 或更高，直接判定不适合随 App 安装包分发。
- [ ] 如果推理依赖 CUDA / H100 / 数据中心 GPU，直接判定不适合普通手机端侧推理。
- [ ] 即使权重能下载，也必须继续用推理数据验证云端方案。

## 5. 最小 256p 推理复现

目标：先证明官方模型能跑通，生成一个最小可用视频。

输入：

- [ ] 官方 demo 图片或参考图。
- [ ] 最短文本 prompt。
- [ ] 默认随机种子。
- [ ] 目标时长：5 秒。
- [ ] 目标分辨率：256p。

执行步骤：

- [ ] 找到官方推理脚本。
- [ ] 使用官方最小命令先跑一次。
- [ ] 如果官方命令缺少参数，按 README 或 demo 配置补齐。
- [ ] 用 `nvidia-smi` 记录推理过程峰值显存。
- [ ] 用 PowerShell `Measure-Command` 记录总耗时。
- [ ] 保存输出视频到 `outputs/smoke-test/`。

命令模板：

```powershell
New-Item -ItemType Directory -Force outputs/smoke-test
Measure-Command {
  python <官方推理脚本> `
    --model_path models/daVinci-MagiHuman `
    --prompt "<最小测试 prompt>" `
    --resolution 256 `
    --duration 5 `
    --output outputs/smoke-test/test_256p.mp4
}
```

实测记录表：

| 指标 | 结果 |
| --- | --- |
| 命令 | 待记录 |
| 输入图片 | 待记录 |
| Prompt | 待记录 |
| 输出视频 | 待记录 |
| 分辨率 | 256p |
| 时长 | 5 秒 |
| 总耗时 | 待记录 |
| 峰值显存 | 待记录 |
| CPU 内存峰值 | 待记录 |
| 是否成功 | 待记录 |
| 失败原因 | 待记录 |

通过标准：

- [ ] 能生成可播放 mp4。
- [ ] 视频长度接近 5 秒。
- [ ] 画面有可识别人物。
- [ ] 音频存在。
- [ ] 没有明显崩溃、黑屏、无声、文件损坏。

## 6. 多语言与输入组合测试

目标：确认文章里提到的多语言和数字人场景是否可复现。

测试用例：

| 编号 | 语言 | 输入 | 目标 |
| --- | --- | --- | --- |
| T01 | 中文普通话 | 短文本 + 参考图 | 基础中文输出 |
| T02 | 英文 | 短文本 + 参考图 | 英文语音输出 |
| T03 | 日语或韩语 | 短文本 + 参考图 | 多语言兼容性 |
| T04 | 中文长文本 | 较长 prompt + 参考图 | 稳定性 |

每个用例记录：

- [ ] 推理命令。
- [ ] 输出路径。
- [ ] 总耗时。
- [ ] 峰值显存。
- [ ] 是否有音频。
- [ ] 口型同步主观评分：1 到 5。
- [ ] 表情自然度主观评分：1 到 5。
- [ ] 语音可懂度主观评分：1 到 5。
- [ ] 失败或异常现象。

## 7. 1080p 与超分测试

目标：验证高分辨率流程和官方 H100 指标之间的差距。

执行顺序：

- [ ] 在 256p 成功后再跑 540p 或官方中间分辨率。
- [ ] 再启用超分流程生成 1080p。
- [ ] 每次只改变一个变量：分辨率、步数、是否超分、是否蒸馏模型。
- [ ] 记录每次测试的显存和耗时。

性能记录表：

| 测试 | 分辨率 | 时长 | 是否超分 | GPU | 显存 | 耗时 | 成功/失败 | 输出 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| P01 | 256p | 5 秒 | 否 | 待记录 | 待记录 | 待记录 | 待记录 | 待记录 |
| P02 | 540p | 5 秒 | 待定 | 待记录 | 待记录 | 待记录 | 待记录 | 待记录 |
| P03 | 1080p | 5 秒 | 是 | 待记录 | 待记录 | 待记录 | 待记录 | 待记录 |

通过标准：

- [ ] 1080p 能完整生成。
- [ ] 输出视频无明显坏帧。
- [ ] 记录当前机器与官方 H100 指标的差距。
- [ ] 明确最低可接受 GPU 配置。

## 8. 手机 App 可行性实验

目标：用复现数据支撑 App 结论，而不是只凭经验判断。

需要回答的问题：

- [ ] 模型权重体积是否适合放进 App 安装包？
- [ ] 推理是否依赖 CUDA / NVIDIA GPU？
- [ ] 是否存在 ONNX / Core ML / NCNN / MNN 可导出路径？
- [ ] 推理耗时是否能达到手机交互可接受范围？
- [ ] 手机端是否只适合做输入采集、任务提交和结果播放？

移动端评估表：

| 评估项 | 实测/核验结果 | 结论 |
| --- | --- | --- |
| 权重大小 | 待记录 | 待判断 |
| 推理后端 | 待记录 | 待判断 |
| 峰值显存 | 待记录 | 待判断 |
| 单次生成耗时 | 待记录 | 待判断 |
| 依赖 CUDA | 待记录 | 待判断 |
| 可导出移动端格式 | 待记录 | 待判断 |
| 手机端电量/发热风险 | 待记录 | 待判断 |
| App 包体积风险 | 待记录 | 待判断 |

最终判断模板：

```markdown
结论：选择 A / B / C

依据：
- 模型权重总大小：xxx
- 单次 256p 推理耗时：xxx
- 单次 1080p 推理耗时：xxx
- 峰值显存：xxx
- 是否依赖 CUDA：是/否
- 是否存在移动端导出路径：是/否
- 输出质量评分：xxx

建议架构：
- 手机端：xxx
- 后端 API：xxx
- GPU Worker：xxx
- 存储：xxx
```

## 9. 服务端 API 原型

目标：如果端侧不可行，验证“手机 App + 云端推理”是否可落地。

最小接口设计：

- [ ] `POST /tasks`：上传参考图、prompt、语言、分辨率，创建生成任务。
- [ ] `GET /tasks/{task_id}`：查询任务状态、进度、错误信息。
- [ ] `GET /tasks/{task_id}/result`：下载或播放生成视频。
- [ ] `DELETE /tasks/{task_id}`：删除任务和素材。

任务状态：

- [ ] `queued`
- [ ] `running`
- [ ] `succeeded`
- [ ] `failed`
- [ ] `canceled`

需要记录：

- [ ] API 请求示例。
- [ ] 推理队列设计。
- [ ] 单 GPU 并发策略。
- [ ] 失败重试策略。
- [ ] 生成结果存储位置。
- [ ] 视频过期清理策略。

## 10. GitHub 同步计划

目标：后续每次进展都能同步到 GitHub，便于追踪。

建议目录结构：

```text
magihuman-mobile-lab/
  README.md
  article.md
  7.15-todolist.md
  docs/
    reproduction-log.md
    mobile-feasibility.md
    api-design.md
  scripts/
    check_env.ps1
    download_models.ps1
    run_smoke_test.ps1
  outputs/
    .gitkeep
  third_party/
    .gitkeep
```

同步规则：

- [ ] 官方源码和大模型权重不要直接提交到 GitHub。
- [ ] `third_party/` 只记录来源和 commit，不提交完整外部仓库。
- [ ] `models/` 不提交，只写下载命令。
- [ ] `outputs/` 不提交大视频，只提交小样例或截图，必要时用 Git LFS。
- [ ] 每次复现进展更新 `docs/reproduction-log.md`。
- [ ] 每次 App 判断更新 `docs/mobile-feasibility.md`。

## 11. 今日交付物

- [x] 阅读并整理 `article.md`。
- [x] 创建详细复现 todo。
- [x] 给项目命名：MagiHuman Mobile Lab。
- [x] 写出 GitHub 项目介绍。
- [ ] 核验官方 README 和模型文件。
- [ ] 检查本机硬件和基础工具。
- [ ] 决定 Docker 或云 GPU 复现路线。
- [ ] 开始最小 256p 推理复现。

