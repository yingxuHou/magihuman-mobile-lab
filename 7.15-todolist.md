# 7.15 TodoList：MagiHuman Mobile Lab

## 项目信息

项目名：MagiHuman Mobile Lab

GitHub 仓库名建议：`magihuman-mobile-lab`

项目介绍：

本项目用于复现和评估开源数字人生成模型 daVinci-MagiHuman。目标不是只跑通官方 demo，而是用可记录、可复查的数据判断它是否能被集成进手机 App。项目会依次完成官方资料核验、环境搭建、最小推理复现、性能测试、显存/耗时/模型体积记录、服务端接口验证，以及移动端部署可行性分析。

当前结论状态：

- [x] 根据文章和官方指标形成初步假设：完整模型大概率不适合手机端侧推理。
- [ ] 该结论还没有本地或云 GPU 实测数据支撑，不能作为最终结论。
- [ ] 最终结论必须基于以下实测数据：模型体积、峰值显存、推理耗时、依赖复杂度、输出质量、服务端封装成本、移动端接入成本。

## 判断标准

端侧 App 可行的定义：

- [ ] 模型和依赖能在 iOS / Android 本机运行。
- [ ] 不依赖远程 GPU 服务。
- [ ] 模型体积、内存占用、耗时、电量消耗在手机上可接受。
- [ ] 输出质量达到可演示标准。

云端 App 可行的定义：

- [ ] 模型可以在 GPU 服务器稳定推理。
- [ ] 手机端通过 API 上传输入并获取结果。
- [ ] 单任务耗时、排队、成本、失败重试机制可接受。
- [ ] 生成结果能被手机端顺畅播放、保存和分享。

最终结论必须写成三选一：

- [ ] A. 可以端侧部署。
- [ ] B. 不能端侧部署，但可以做“手机 App + 云端 GPU 推理”。
- [ ] C. 当前复现成本或效果不达标，暂不适合做产品化 App。

## 0. 官方资料核验

目标：确认复现依据来自官方资料，不只依赖转述文章。

- [x] 已阅读 `article.md`。
- [x] 核验论文：https://arxiv.org/abs/2603.21986
- [x] 核验代码：https://github.com/GAIR-NLP/daVinci-MagiHuman
- [x] 核验模型：https://huggingface.co/GAIR/daVinci-MagiHuman
- [x] 核验在线试用：https://huggingface.co/spaces/SII-GAIR/daVinci-MagiHuman
- [x] 记录官方仓库 commit hash。
- [x] 记录模型仓库最后更新时间。
- [x] 记录 license 和商用限制。
- [x] 记录官方推荐的 Python、CUDA、PyTorch、MagiCompiler、flash-attention 版本。
- [x] 找到官方推理入口脚本和示例命令。
- [x] 找到官方 demo 输入素材格式：图片、文本、音频、视频尺寸、输出目录。

资料核验记录表：

| 项目 | 结果 | 备注 |
| --- | --- | --- |
| 论文标题 | Speed by Simplicity: A Single-Stream Architecture for Fast Audio-Video Generative Foundation Model | arXiv:2603.21986 |
| 代码仓库 commit | `209209b7086eba2020c5439265221495a8357322` | GitHub `main` |
| 模型仓库 sha | `7fe95e50c11bd92bdadf94cd51dbec18427f8e0c` | Hugging Face |
| 模型最后更新时间 | 2026-03-25 14:00:27 UTC | Hugging Face API |
| Demo Space sha | `f4ca1ddf0ab78843686894301a8d0d7ec2cf027b` | Hugging Face Space |
| 模型 license | Apache 2.0 | README badge and HF tag |
| 权重文件总大小 | 约 201.27 GiB | HF `blobs=true` API，56 个文件 |
| 最大单文件 | `base/model-00003-of-00007.safetensors`，约 4.66 GiB | HF API |
| 官方最小推理命令 | `bash example/base/run_T2V.sh` | 256p T2V |
| 官方推理入口 | `inference/pipeline/entry.py` | README |
| 官方推荐硬件 | 单块 H100 给出官方速度指标；consumer GPU 需要 CPU offload 配置 | README 示例脚本注释 |
| 官方 H100 速度指标 | 5 秒视频：256p 2.0s，540p 8.0s，1080p 38.4s | README performance table |

通过标准：

- [x] 能明确知道要下载哪些权重。
- [x] 能明确知道运行哪个推理脚本。
- [x] 能明确知道最小 demo 需要哪些输入文件。

## 1. 本机硬件与工具检查

目标：先判断当前机器是否值得直接复现，避免在不合适的环境里浪费时间。

执行命令：

```powershell
nvidia-smi
nvcc --version
git --version
git lfs version
conda --version
docker --version
ffmpeg -version
python --version
```

硬件记录表：

| 项目 | 结果 |
| --- | --- |
| 操作系统 | Microsoft Windows 11 Home China 10.0.26200，64-bit |
| GPU 型号 | Intel(R) Iris(R) Xe Graphics |
| GPU 数量 | 1 个 Windows 显示控制器；未检测到 NVIDIA GPU |
| 单卡显存 | 约 2 GiB 共享/显存报告值 |
| NVIDIA Driver | 未检测到，`nvidia-smi` 不可用 |
| CUDA Runtime | 未检测到 |
| CUDA Toolkit | 未检测到，`nvcc` 不可用 |
| Docker 是否可用 | 不可用，`docker` 命令不存在 |
| Conda 是否可用 | 不可用，`conda` 命令不存在 |
| Git | `git version 2.50.1.windows.1` |
| Git LFS | `git-lfs/3.7.0` |
| ffmpeg | `ffmpeg version 8.0.1-full_build-www.gyan.dev` |
| Python | `Python 3.8.6`，未安装 torch |
| 磁盘可用空间 | D 盘约 169.36 GiB 可用 |

决策：

- [x] 如果没有 NVIDIA GPU，直接转云 GPU。
- [x] 如果显存明显不足，仍可做代码阅读和接口设计，但不做本地完整推理。
- [ ] 如果 Docker 可用，优先走 Docker 路线。
- [ ] 如果 Docker 不可用，再尝试 Conda 本地路线。

当前决策：本机不具备本地完整复现条件。后续可以在本机继续做官方源码检查、脚本准备和报告同步；真正推理应切到云 GPU 或另一台 NVIDIA GPU 机器。

## 2. 复现路线选择

实际推理首选路线：云 GPU + Docker

原因：

- [x] 当前官方 README 给出了 `sandai/magi-human:latest` Docker 镜像。
- [x] MagiCompiler、CUDA、flash-attention、PyTorch 版本容易冲突，Docker 更容易复现。
- [x] 后续服务端部署也更适合容器化。
- [x] 当前本机没有 NVIDIA GPU、Docker、Conda 和 Python 3.12，不适合做完整推理。

备选路线：Conda

使用条件：

- [ ] Docker 不可用。
- [ ] 需要调试源码。
- [ ] 当前机器 CUDA / PyTorch / 编译工具链版本可控。

Docker 路线待执行：

```powershell
docker pull sandai/magi-human:latest
docker images | Select-String magi-human
```

Conda 路线待执行：

```powershell
conda create -n davinci-magihuman python=3.12
conda activate davinci-magihuman
conda install ffmpeg
```

通过标准：

- [ ] 能进入可用 Python 环境。
- [ ] 能 import torch。
- [ ] 能识别 GPU。
- [ ] 能编译或加载 MagiCompiler 相关依赖。

当前路线决策：

- [x] 本机不走完整本地推理路线。
- [x] 本机继续做代码准备、脚本准备、文档和 GitHub 同步。
- [x] 需要准备云 GPU 复现环境，优先 H100，其次多卡高显存 NVIDIA GPU；A100/4090/5090 需要按官方 README 的 CPU offload 或 `cp_size` 配置重新评估。
- [ ] 在云 GPU 上执行 `docker pull sandai/magi-human:latest`。
- [ ] 在云 GPU 上执行模型下载和 256p smoke test。

## 3. 代码准备

目标：保持官方代码独立，便于后续同步和记录改动。

执行步骤：

- [x] 在本仓库下创建 `third_party/`。
- [x] 克隆官方代码到 `third_party/daVinci-MagiHuman`。
- [x] 克隆 MagiCompiler 到 `third_party/MagiCompiler`，或者使用 Docker 镜像内置版本。
- [x] 记录两个仓库的 commit hash。
- [x] 不直接修改官方源码；如必须修改，先用 patch 文件记录。

命令模板：

```powershell
New-Item -ItemType Directory -Force third_party
git clone https://github.com/GAIR-NLP/daVinci-MagiHuman third_party/daVinci-MagiHuman
git clone https://github.com/SandAI-org/MagiCompiler third_party/MagiCompiler
git -C third_party/daVinci-MagiHuman rev-parse HEAD
git -C third_party/MagiCompiler rev-parse HEAD
```

记录表：

| 仓库 | commit | 是否修改 | 备注 |
| --- | --- | --- | --- |
| daVinci-MagiHuman | `209209b7086eba2020c5439265221495a8357322` | 否 | 位于 `third_party/daVinci-MagiHuman`，不提交到本仓库 |
| MagiCompiler | `bfef5bc70226a0c0740e4c551e4f7245a974fb4f` | 否 | 位于 `third_party/MagiCompiler`，不提交到本仓库 |

## 4. 模型权重准备

目标：下载并记录所有必须权重，确认模型体积是否已经超出手机 App 端侧可接受范围。

执行步骤：

- [ ] 安装 Hugging Face CLI。
- [x] 登录 Hugging Face，确认是否需要申请权限。
- [ ] 下载 `GAIR/daVinci-MagiHuman` 到本地模型目录。
- [x] 记录权重总大小。
- [x] 记录单个最大文件大小。
- [x] 记录是否包含基础模型、蒸馏模型、超分模型、VAE、tokenizer、配置文件。

当前状态：本机不下载完整权重。原因是无 GPU、无 Docker/Conda，且完整官方模型栈加外部模型约 285.63 GiB，不含缓存和输出；当前 D 盘约 169.36 GiB 可用。下载应在云 GPU 环境执行。

命令模板：

```powershell
pip install -U huggingface_hub
huggingface-cli login
huggingface-cli download GAIR/daVinci-MagiHuman --local-dir models/daVinci-MagiHuman
Get-ChildItem models/daVinci-MagiHuman -Recurse | Measure-Object -Property Length -Sum
Get-ChildItem models/daVinci-MagiHuman -Recurse | Sort-Object Length -Descending | Select-Object -First 20 FullName,Length
```

模型记录表：

| 项目 | 结果 |
| --- | --- |
| daVinci-MagiHuman 权重总大小 | 约 201.27 GiB |
| daVinci-MagiHuman 最大单文件 | `base/model-00003-of-00007.safetensors`，约 4.66 GiB |
| daVinci-MagiHuman 文件数量 | 56 |
| base | 约 28.54 GiB |
| distill | 约 56.99 GiB |
| 540p_sr | 约 56.99 GiB |
| 1080p_sr | 约 56.99 GiB |
| turbo_vae | 约 1.74 GiB |
| 外部 Text Encoder | `google/t5gemma-9b-9b-ul2`，约 37.91 GiB，manual gated |
| 外部 Audio Model | `stabilityai/stable-audio-open-1.0`，约 14.60 GiB，auto gated |
| 外部 VAE | `Wan-AI/Wan2.2-TI2V-5B`，约 31.85 GiB |
| 完整依赖估算 | 约 285.63 GiB，不含缓存、日志、输出视频 |
| 是否需要 Git LFS | 模型仓库使用 LFS/safetensors 大文件 |
| 是否需要额外外部权重 | 是 |
| 是否可接受放进 App 包 | 否 |

判断规则：

- [x] 如果权重达到几十 GB 或更高，直接判定不适合随 App 安装包分发。
- [x] 如果推理依赖 CUDA / H100 / 数据中心 GPU，直接判定不适合普通手机端侧推理。
- [ ] 即使权重能下载，也必须继续用推理数据验证云端方案。

## 5. 最小 256p 推理复现

目标：先证明官方模型能跑通，生成一个最小可用视频。

输入：

- [x] 官方 demo 图片或参考图：P01 为 T2V，不使用参考图；官方 TI2V 参考图 hash 已记录在 `docs/p01-smoke-manifest.json`。
- [x] 最短文本 prompt：P01 使用 `backend.experiment_matrix` 中的短英文 prompt，hash 已记录。
- [x] 默认随机种子：P01 显式传入 `MAGIHUMAN_SEED=42`。
- [x] 目标时长：5 秒。
- [x] 目标分辨率：256p。

执行步骤：

- [x] 找到官方推理脚本。
- [ ] 使用官方最小命令先跑一次。
- [x] 如果官方命令缺少参数，按 README 或 demo 配置补齐。
- [ ] 用 `nvidia-smi` 记录推理过程峰值显存。
- [ ] 用 PowerShell `Measure-Command` 或 Linux `/usr/bin/time -v` 记录总耗时。
- [ ] 保存输出视频到 `outputs/smoke-test/`。
- [x] 准备显存、耗时、视频有效性解析工具。

当前状态：已准备手动 smoke 脚本 `scripts/run_base_t2v_smoke.sh` 和 worker 兼容脚本 `scripts/magihuman_task_runner.sh`，并新增 `docs/p01-smoke-manifest.json` 锁定 P01 输入、seed、时长、分辨率、官方源码 commit 和预期输出路径；但当前本机没有 NVIDIA GPU/CUDA/Docker/Conda，无法执行推理。该步骤必须在云 GPU 或远程 NVIDIA GPU 机器上继续。

命令模板：

```powershell
New-Item -ItemType Directory -Force outputs/smoke-test
Measure-Command {
  python <官方推理脚本> `
    --model_path models/daVinci-MagiHuman `
    --prompt "<最小测试 prompt>" `
    --resolution 256 `
    --duration 5 `
    --output outputs/smoke-test/test_256p.mp4
}
```

实测记录表：

| 指标 | 结果 |
| --- | --- |
| 命令 | 待在 GPU 环境执行 `MODEL_ROOT=models bash scripts/magihuman_task_runner.sh` 后记录 |
| 输入图片 | T2V 模式暂不使用参考图；TI2V 后续使用 `example/assets/image.png` |
| Prompt | P01 实际执行 prompt 来自 `backend.experiment_matrix`；官方 `example/assets/prompt.txt` 作为 fallback/hash 记录在 manifest |
| 输出视频 | 待 GPU 环境生成 |
| 分辨率 | 256p |
| 时长 | P01 实际执行目标为 5 秒；官方 base 示例脚本为 4 秒 |
| 总耗时 | 待记录 |
| 峰值显存 | 待记录 |
| CPU 内存峰值 | 待记录 |
| 视频有效性 | 待 GPU 环境生成后用 `python -m backend.run_metrics` 和 `ffprobe` 记录 |
| 是否成功 | 未执行 |
| 失败原因 | 当前本机缺少 NVIDIA GPU/CUDA/Docker/Conda |

通过标准：

- [ ] 能生成可播放 mp4。
- [ ] 视频长度接近 5 秒。
- [ ] 画面有可识别人物。
- [ ] 音频存在。
- [ ] 没有明显崩溃、黑屏、无声、文件损坏。

## 6. 多语言与输入组合测试

目标：确认文章里提到的多语言和数字人场景是否可复现。

当前状态：已准备 `backend/experiment_matrix.py` 和 `run_configs/experiment_matrix.json` 生成逻辑，覆盖中文、英文、日文、韩文 TI2V 测试；还没有在 GPU 上执行。
已准备 `backend/experiment_runner.py`，可按 case id 执行并检查依赖。

测试用例：

| 编号 | 语言 | 输入 | 目标 |
| --- | --- | --- | --- |
| T01 | 中文普通话 | 短文本 + 参考图 | 基础中文输出 |
| T02 | 英文 | 短文本 + 参考图 | 英文语音输出 |
| T03 | 日语 | 短文本 + 参考图 | 多语言兼容性 |
| T04 | 韩语 | 短文本 + 参考图 | 多语言兼容性 |

每个用例记录：

- [ ] 推理命令。
- [ ] 输出路径。
- [ ] 总耗时。
- [ ] 峰值显存。
- [ ] 是否有音频。
- [ ] 口型同步主观评分：1 到 5。
- [ ] 表情自然度主观评分：1 到 5。
- [ ] 语音可懂度主观评分：1 到 5。
- [ ] 失败或异常现象。
- [x] 准备多语言实验矩阵和命令生成逻辑。
- [x] 准备实验结果汇总工具，缺少 metrics 的 case 会被标记为 `missing_metrics`。
- [x] 准备按 case 执行的实验 runner。

## 7. 1080p 与超分测试

目标：验证高分辨率流程和官方 H100 指标之间的差距。

当前状态：已准备 P01/P02/P03/P04 性能实验矩阵，其中 P01=256p base，P02=256p distill，P03=540p SR，P04=1080p SR；还没有在 GPU 上执行。
已准备依赖检查：P03 依赖 P01，P04 依赖 P01 和 P03。

执行顺序：

- [ ] 在 256p 成功后再跑 540p 或官方中间分辨率。
- [ ] 再启用超分流程生成 1080p。
- [ ] 每次只改变一个变量：分辨率、步数、是否超分、是否蒸馏模型。
- [ ] 记录每次测试的显存和耗时。
- [x] 准备 `backend/run_metrics.py` 统一解析 `nvidia-smi`、`/usr/bin/time -v` 和 `ffprobe` 输出。
- [x] 准备性能实验矩阵和依赖顺序。
- [x] 准备 `backend/experiment_results.py` 汇总 P01/P03/P04 的 metrics。
- [x] 准备 `backend/experiment_runner.py` 执行单个 case 并阻止依赖未满足的误跑。

性能记录表：

| 测试 | 分辨率 | 时长 | 是否超分 | GPU | 显存 | 耗时 | 成功/失败 | 输出 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| P01 | 256p | 5 秒 | 否 | 待记录 | 待记录 | 待记录 | 待记录 | 待记录 |
| P02 | 540p | 5 秒 | 待定 | 待记录 | 待记录 | 待记录 | 待记录 | 待记录 |
| P03 | 1080p | 5 秒 | 是 | 待记录 | 待记录 | 待记录 | 待记录 | 待记录 |

通过标准：

- [ ] 1080p 能完整生成。
- [ ] 输出视频无明显坏帧。
- [ ] 记录当前机器与官方 H100 指标的差距。
- [ ] 明确最低可接受 GPU 配置。

## 8. 手机 App 可行性实验

目标：用复现数据支撑 App 结论，而不是只凭经验判断。

需要回答的问题：

- [x] 模型权重体积是否适合放进 App 安装包？
- [x] 推理是否依赖 CUDA / NVIDIA GPU？
- [x] 是否存在 ONNX / Core ML / NCNN / MNN 可导出路径？
- [ ] 推理耗时是否能达到手机交互可接受范围？
- [x] 手机端是否只适合做输入采集、任务提交和结果播放？

当前静态代码结论：官方仓库没有发现 ONNX / Core ML / NCNN / MNN / TFLite / TorchScript 移动导出路径。官方示例依赖 `torchrun`、CUDA/NCCL、MagiCompiler、Flash Attention，并在 SR 脚本中使用 CPU offload。移动端更合理的角色是任务提交和结果播放。

移动端评估表：

| 评估项 | 实测/核验结果 | 结论 |
| --- | --- | --- |
| 权重大小 | 官方完整依赖约 285.63 GiB；base smoke-test 依赖约 114.64 GiB | 不适合 App 包内分发 |
| 推理后端 | 官方路径为 PyTorch / torchrun / CUDA / NCCL / MagiCompiler | 不适合普通手机端侧 |
| 峰值显存 | 待记录 | 待判断 |
| 单次生成耗时 | 待记录 | 待判断 |
| 依赖 CUDA | 官方 README 和脚本均指向 NVIDIA CUDA 运行环境 | 端侧不可行倾向强 |
| 可导出移动端格式 | 未发现 ONNX / Core ML / NCNN / MNN / TFLite / TorchScript 导出路径 | 官方栈暂无移动端导出路线 |
| 手机端电量/发热风险 | 未实测，但官方 H100 指标和模型体积说明端侧风险极高 | 不建议端侧推理 |
| App 包体积风险 | 完整依赖约 285.63 GiB | 不可接受 |

最终判断模板：

```markdown
结论：选择 A / B / C

依据：
- 模型权重总大小：xxx
- 单次 256p 推理耗时：xxx
- 单次 1080p 推理耗时：xxx
- 峰值显存：xxx
- 是否依赖 CUDA：是/否
- 是否存在移动端导出路径：是/否
- 输出质量评分：xxx

建议架构：
- 手机端：xxx
- 后端 API：xxx
- GPU Worker：xxx
- 存储：xxx
```

## 9. 服务端 API 原型

目标：如果端侧不可行，验证“手机 App + 云端推理”是否可落地。

最小接口设计：

- [x] `POST /tasks`：上传参考图、prompt、语言、分辨率，创建生成任务。
- [x] `GET /tasks/{task_id}`：查询任务状态、进度、错误信息。
- [x] `GET /tasks/{task_id}/result`：下载或播放生成视频。
- [x] `DELETE /tasks/{task_id}`：删除任务和素材。

任务状态：

- [x] `queued`
- [x] `running`
- [x] `succeeded`
- [x] `failed`
- [x] `canceled`

需要记录：

- [x] API 请求示例。
- [x] 推理队列设计。
- [x] 单 GPU 并发策略。
- [x] 失败处理策略。
- [x] 自动失败重试策略。
- [x] 生成结果存储位置。
- [x] 视频过期清理策略。

当前实现：已添加标准库 API 原型 `backend/api_server.py`、`backend/task_store.py`、worker 骨架 `backend/worker.py`、MagiHuman runner 配置模块 `backend/magihuman_config.py` 和指标解析模块 `backend/run_metrics.py`，本地通过 `python -m unittest discover -s tests -v` 的 18 个测试。该原型不执行真实 GPU 推理，只验证手机端到服务端的任务接口、worker 状态流转、官方配置生成逻辑和运行指标解析逻辑。
结果聚合：已添加 `backend/experiment_results.py`，可把 `logs/*_metrics.json` 汇总成 Markdown 表；当前没有 GPU metrics 时会明确输出 `insufficient_runtime_evidence`。
重试与清理：已添加 worker 自动重试和 `backend/retention.py`，可按 TTL 清理已生成结果文件。

## 10. GitHub 同步计划

目标：后续每次进展都能同步到 GitHub，便于追踪。

建议目录结构：

```text
magihuman-mobile-lab/
  README.md
  article.md
  7.15-todolist.md
  docs/
    reproduction-log.md
    mobile-feasibility.md
    api-design.md
  scripts/
    check_env.ps1
    download_models.ps1
    run_smoke_test.ps1
  outputs/
    .gitkeep
  third_party/
    .gitkeep
```

同步规则：

- [ ] 官方源码和大模型权重不要直接提交到 GitHub。
- [ ] `third_party/` 只记录来源和 commit，不提交完整外部仓库。
- [ ] `models/` 不提交，只写下载命令。
- [ ] `outputs/` 不提交大视频，只提交小样例或截图，必要时用 Git LFS。
- [ ] 每次复现进展更新 `docs/reproduction-log.md`。
- [ ] 每次 App 判断更新 `docs/mobile-feasibility.md`。

## 11. 今日交付物

- [x] 阅读并整理 `article.md`。
- [x] 创建详细复现 todo。
- [x] 给项目命名：MagiHuman Mobile Lab。
- [x] 写出 GitHub 项目介绍。
- [x] 核验官方 README 和模型文件。
- [x] 检查本机硬件和基础工具。
- [x] 决定 Docker 或云 GPU 复现路线。
- [ ] 开始最小 256p 推理复现。

## 12. 手机 App 可行性判定器

目标：把“能不能装进手机 App”做成可重复执行的判断流程，而不是只写一段主观结论。

- [x] 新增 `backend/feasibility_decision.py`。
- [x] 把本 todo 中的 A/B/C 三种结论写成程序化状态。
- [x] 纳入静态证据：完整依赖约 285.63 GiB、base smoke-test 依赖约 114.64 GiB、CUDA/服务端 GPU 依赖、官方源码未发现移动端导出路径。
- [x] 纳入运行证据：读取 `logs/*_metrics.json`，确认哪些 GPU 实验已有实测数据。
- [x] 把 P01、P03、P04、T01、T02 设为最终判断前必须补齐的 GPU 实测 case。
- [x] 当前自动输出为 `B_pending_runtime`：官方端侧部署不可行，云端 GPU 方案还缺实测。
- [x] 增加缺失 metrics 和已补齐 metrics 两种状态的本地测试。
- [ ] 在 Linux NVIDIA GPU 主机上运行 P01/P03/P04/T01/T02。
- [ ] GPU metrics 收集后重新运行 `python -m backend.feasibility_decision --log-dir logs --format markdown`。
- [ ] 只有在延迟、显存、输出质量和成本复核后，才能把 `B_pending_runtime` 改成最终 B 或 C。

## 13. 必跑 GPU 实验套件

目标：把最终判断前必须实测的 case 串成一个可以在 GPU 主机上一键执行的套件。

- [x] 新增 `backend/experiment_suite.py`。
- [x] 默认套件顺序：P01、P03、P04、T01、T02。
- [x] 支持 `--include-optional` 追加 P02、T03、T04。
- [x] 支持 dry-run 输出 shell 命令。
- [x] 支持 `--execute` 在 Linux NVIDIA GPU 主机上按顺序执行。
- [x] 如果某个 case 已有有效 metrics，默认跳过；需要重跑时使用 `--rerun`。
- [x] 增加依赖阻断测试：只选 P04 且没有 P01/P03 metrics 时会标记 blocked。
- [ ] 在 GPU 主机上运行 `bash scripts/run_experiment_suite.sh --execute`。
- [ ] 执行后运行 `python -m backend.experiment_results --log-dir logs --format markdown` 汇总指标。
- [ ] 执行后运行 `python -m backend.feasibility_decision --log-dir logs --format markdown` 更新 App 判断。

## 14. GPU 主机预检与一键流水线

目标：在真正的 Linux NVIDIA GPU 主机上，把预检、模型下载、必跑实验、结果汇总和 App 判定串成可审计流程。

- [x] 新增 `backend/gpu_preflight.py`。
- [x] 支持 `host` 模式检查 `nvidia-smi`、Docker、Git、Git LFS、Python、Bash。
- [x] 支持 `container` 模式检查 `nvidia-smi`、`torchrun`、Python、Bash、ffprobe、官方源码目录和模型目录。
- [x] 支持输出 JSON 和 Markdown 预检报告。
- [x] 新增 `scripts/gpu_reproduction_pipeline.sh`。
- [x] 流水线会生成预检报告、实验 suite dry-run 或执行日志、实验汇总报告、可行性判定报告。
- [x] 本机预检结果为 `not_ready`，缺少 NVIDIA/CUDA 执行条件和模型目录。
- [ ] 在 GPU 主机上运行 `bash scripts/gpu_reproduction_pipeline.sh` 先做 dry-run。
- [ ] 在 GPU 主机上运行 `DOWNLOAD_MODELS=1 EXECUTE=1 bash scripts/gpu_reproduction_pipeline.sh` 做真实复现。
- [ ] 把 GPU 主机生成的报告内容同步回 `docs/reproduction-log.md` 和 `docs/mobile-feasibility.md`。

## 15. 生成样片质量评审门槛

目标：GPU metrics 只能证明“跑出了文件”，不能证明样片适合放进手机 App。最终判断必须补充结构化质量评审。

- [x] 新增 `backend/quality_review.py`。
- [x] 支持生成 P01/P03/P04/T01/T02 的质量评审 JSON 模板。
- [x] 评审字段包括手机播放、音画同步、脸部质量、动作自然度、语音可懂度、伪影控制和备注。
- [x] 评分范围为 1 到 5，最低通过分为 3。
- [x] 支持汇总为 Markdown 或 JSON。
- [x] `backend/feasibility_decision.py` 已接入 `--quality-review`。
- [x] 如果 GPU metrics 已齐但质量评审缺失，判定只能到 `B_candidate_needs_quality_review`。
- [x] 如果质量评审通过，判定推进到 `B_candidate_needs_cost_review`。
- [x] 如果质量评审失败，判定推进到 `C_candidate_quality_failed`。
- [ ] GPU 样片生成后运行 `python -m backend.quality_review --create-template --output docs/quality-review.json`。
- [ ] 人工填写 P01/P03/P04/T01/T02 的质量评审。
- [ ] 用 `python -m backend.feasibility_decision --log-dir logs --quality-review docs/quality-review.json --format markdown` 更新最终 App 判断。

## 16. 云端 GPU 成本与等待时间评审门槛

目标：即使模型跑通且质量通过，也必须确认单条视频成本和等待时间是否适合手机 App 产品化。

- [x] 新增 `backend/cost_review.py`。
- [x] 支持生成 `docs/cost-review.json` 模板。
- [x] 成本公式：`wall_time_seconds / 3600 * gpu_hourly_usd * billing_overhead_multiplier`。
- [x] 评审字段包括 GPU 小时价、计费/排队开销倍数、单条视频最大可接受成本、最大等待秒数。
- [x] 支持汇总为 Markdown 或 JSON。
- [x] `backend/feasibility_decision.py` 已接入 `--cost-review`。
- [x] 如果质量通过但成本评审缺失，判定只能到 `B_candidate_needs_cost_review`。
- [x] 如果成本评审通过，判定推进到 `B_candidate_ready_for_product_review`。
- [x] 如果成本或等待时间超标，判定推进到 `C_candidate_cost_failed`。
- [ ] GPU metrics 生成后运行 `python -m backend.cost_review --create-template --output docs/cost-review.json`。
- [ ] 填写选定 GPU 供应商的小时价和产品阈值。
- [ ] 用 `python -m backend.feasibility_decision --log-dir logs --quality-review docs/quality-review.json --cost-review docs/cost-review.json --format markdown` 更新最终 App 判断。

## 17. 最终可行性报告生成器

目标：把静态证据、GPU runtime、质量评审、成本评审和最终 A/B/C 判断合并成一个 GitHub 可读报告。

- [x] 新增 `backend/final_report.py`。
- [x] 支持 Markdown 和 JSON 输出。
- [x] 报告包含 Evidence Gates：端侧静态可行性、GPU runtime、生成质量、云 GPU 成本/等待时间。
- [x] 报告包含 runtime metrics 表、quality review 表、cost review 表。
- [x] 当前已生成 `docs/mobile-feasibility-report.md`。
- [x] `scripts/gpu_reproduction_pipeline.sh` 已输出 `outputs/reports/final_report_<timestamp>.md`。
- [ ] GPU 实测、质量评审、成本评审补齐后，重新生成 `docs/mobile-feasibility-report.md`。
- [ ] 把最终报告中的 `B_pending_runtime` 更新为最终 B 或 C。

## 18. GPU 证据包导入与校验

目标：GPU 主机上生成的 metrics、报告和评审 JSON 要能安全带回 GitHub 项目；大视频和模型权重不能被提交。

- [x] 新增 `scripts/package_gpu_evidence.sh`。
- [x] 证据包只包含 `logs/*_metrics.json`、`logs/*artifact_audit*.json`、`docs/quality-review.json`、`docs/cost-review.json`、P01 manifest、provenance 和小报告文件。
- [x] 证据包不包含 mp4、模型权重、第三方源码。
- [x] 新增 `backend/evidence_import.py`。
- [x] 支持校验 runtime、quality、cost 三类证据是否完整。
- [x] 当前已生成 `docs/gpu-evidence-import-audit.md`，状态为 `incomplete_import`。
- [ ] GPU 主机跑完后执行 `bash scripts/package_gpu_evidence.sh`。
- [ ] 把证据包解压回本项目根目录。
- [ ] 运行 `python -m backend.evidence_import --log-dir logs --quality-review docs/quality-review.json --cost-review docs/cost-review.json --final-report-output docs/mobile-feasibility-report.md --format markdown --output docs/gpu-evidence-import-audit.md`。
- [ ] 导入校验通过后重新生成并提交 `docs/mobile-feasibility-report.md`。

## 19. 云 GPU 主机 Bootstrap 与官方源码锁定

目标：把新的云 GPU 主机初始化流程固定下来，避免每次手动复制命令，同时确保官方源码版本可复查。

- [x] 新增 `backend/gpu_bootstrap.py`，生成 GPU 主机命令、容器内命令、Docker 启动命令和源码锁定信息。
- [x] 新增 `scripts/prepare_sources.sh`，在容器或 GPU 主机内克隆/更新官方 daVinci-MagiHuman 与 MagiCompiler 源码。
- [x] 锁定 daVinci-MagiHuman commit：`209209b7086eba2020c5439265221495a8357322`。
- [x] 锁定 MagiCompiler commit：`bfef5bc70226a0c0740e4c551e4f7245a974fb4f`。
- [x] 生成挂载项目根目录到 `/repo` 的 Docker 命令，默认镜像为 `sandai/magi-human:latest`。
- [x] 新增 `scripts/bootstrap_gpu_host.sh`，用于生成 host preflight 报告、bootstrap 计划和 `outputs/run_magi_container.sh`。
- [x] `scripts/bootstrap_gpu_host.sh` 默认生成 `outputs/reports/upstream_drift_audit.md/json`，确认官方上游元数据仍匹配锁定版本。
- [x] 本地测试通过：`python -m unittest tests.test_gpu_bootstrap -v`。
- [ ] 在 Linux NVIDIA GPU 主机上运行 `bash scripts/bootstrap_gpu_host.sh`。
- [ ] 检查 `outputs/reports/upstream_drift_audit.md` 状态为 `locked_current`。
- [ ] 在生成的容器里运行 `INSTALL_MAGICOMPILER=1 bash scripts/prepare_sources.sh`。
- [ ] 在生成的容器里运行 `DOWNLOAD_MODELS=1 EXECUTE=1 bash scripts/gpu_reproduction_pipeline.sh`。
- [ ] 跑完后执行 `bash scripts/package_gpu_evidence.sh`，把小证据包带回本项目并重新生成最终报告。

## 20. GPU 流水线加固：HF 权限、源码准备、下载后复检

目标：减少云 GPU 第一次真实复现时的失败点，尤其是 gated 模型权限、源码目录缺失和模型下载不完整。

- [x] `backend/gpu_preflight.py` 新增 Hugging Face auth 检查。
- [x] `backend/gpu_preflight.py` 新增 `--strict` 模式，必需检查失败时直接返回非零状态。
- [x] container 预检新增 `git` 检查，因为 `scripts/prepare_sources.sh` 需要拉取官方源码。
- [x] `scripts/gpu_reproduction_pipeline.sh` 默认先执行 `scripts/prepare_sources.sh`，再做 container preflight。
- [x] 当 `DOWNLOAD_MODELS=1` 时，pipeline 会要求 `HF_TOKEN`、`HUGGINGFACE_HUB_TOKEN` 或 `huggingface-cli login` token 缓存。
- [x] 新增 `backend/hf_access_audit.py`，通过 HEAD probe 检查 P01/required-suite 所需的 Hugging Face 代表性权重文件访问权限。
- [x] `scripts/run_p01_smoke_pipeline.sh` 会在 `MODEL_PROFILE=p01` 下载前运行 `backend.hf_access_audit --profile p01 --strict`。
- [x] `scripts/gpu_reproduction_pipeline.sh` 会在 required-suite 下载前运行 `backend.hf_access_audit --profile required_suite --strict`。
- [x] 当模型下载完成后，pipeline 会再次运行 require-models 预检，确认四个模型目录存在。
- [x] `backend/gpu_bootstrap.py` 生成的 Docker 命令会透传 `HF_TOKEN` 和 `HUGGINGFACE_HUB_TOKEN` 到容器。
- [x] 本地 dry-run 验证通过：`PREPARE_SOURCES=0 MIN_DISK_GIB=0 bash scripts/gpu_reproduction_pipeline.sh`。
- [x] 本地 HF access audit 测试通过：`python -m unittest tests.test_hf_access_audit tests.test_pipeline_scripts tests.test_p01_smoke_pipeline -v`。
- [ ] 在 GPU 主机上设置 `export HF_TOKEN=<token>`。
- [ ] 在 GPU 主机上运行 `python -m backend.hf_access_audit --profile p01 --format markdown`，确认 `google/t5gemma-9b-9b-ul2` 和 `stabilityai/stable-audio-open-1.0` 的 gated probe 为 `accessible`。
- [ ] 在 GPU 主机上运行 `python -m backend.hf_access_audit --profile required_suite --format markdown`，确认 SR 权重 probe 也可访问。
- [ ] 在 GPU 主机上运行 `bash scripts/bootstrap_gpu_host.sh && bash outputs/run_magi_container.sh`。
- [ ] 在容器内运行 `INSTALL_MAGICOMPILER=1 DOWNLOAD_MODELS=1 EXECUTE=1 bash scripts/gpu_reproduction_pipeline.sh`，确认 strict preflight 和 post-download preflight 都通过。

## 21. 最小 256p P01 Smoke Pipeline

目标：第一次真实 GPU 复现只跑最小 P01 case，先确认 256p base T2V 能生成可播放视频和 metrics，再进入 P03/P04/T01/T02。

- [x] 新增 `scripts/run_p01_smoke_pipeline.sh`。
- [x] P01 smoke pipeline 复用 `backend.experiment_suite --cases P01`，不单独发明一套实验参数。
- [x] P01 smoke pipeline 包含源码准备、HF auth 检查、可选模型下载、下载后模型目录复检。
- [x] P01 smoke pipeline 会生成 P01 plan、experiment summary、feasibility decision 和 final report。
- [x] P01 smoke pipeline 在 `EXECUTE=1` 后自动生成 acceptance gate，阻断证据不完整的 full suite。
- [x] 本地 dry-run 通过：`PREPARE_SOURCES=0 MIN_DISK_GIB=0 bash scripts/run_p01_smoke_pipeline.sh`，输出 `not_ready` 原因为本机没有 `nvidia-smi`/`torchrun`。
- [x] 本地测试通过：`python -m unittest tests.test_p01_smoke_pipeline -v`。
- [ ] 在 Linux NVIDIA GPU 容器内运行 `INSTALL_MAGICOMPILER=1 DOWNLOAD_MODELS=1 EXECUTE=1 bash scripts/run_p01_smoke_pipeline.sh`。
- [ ] 确认生成 `outputs/smoke-test/P01.mp4`。
- [ ] 确认生成 `logs/*P01*_metrics.json`，并记录 GPU、峰值显存、wall time、视频/音频元数据。
- [ ] 确认生成 `outputs/reports/p01_acceptance_*.md`，且状态为 `ready_for_full_suite` 或 `ready_for_full_suite_with_transcode_required`。
- [ ] P01 成功后再运行 full suite：`EXECUTE=1 bash scripts/gpu_reproduction_pipeline.sh`。

## 22. 移动端视频播放兼容性 Gate

目标：即使 GPU 成功生成 mp4，也要确认它是否适合手机 App 直接播放、保存、分享；否则需要转码策略。

- [x] 扩展 `backend/run_metrics.py`，从 `ffprobe` 保存视频编码、音频编码、像素格式、码率等字段。
- [x] 新增 `backend/mobile_video_check.py`。
- [x] 检查项包括 MP4/MOV 容器、H.264 视频、`yuv420p` 像素格式、AAC 音频、分辨率上限、文件大小上限。
- [x] 当视频不满足直接移动端播放要求时，输出 `mobile_video_needs_transcode` 和参考 `ffmpeg` 转码命令。
- [x] `scripts/gpu_reproduction_pipeline.sh` 和 `scripts/run_p01_smoke_pipeline.sh` 已自动生成 mobile video compatibility 报告。
- [x] `docs/mobile-feasibility-report.md` 已新增 mobile video playback compatibility evidence gate。
- [x] 本地测试通过：`python -m unittest tests.test_mobile_video_check -v`。
- [ ] GPU 生成 P01 后运行 `python -m backend.mobile_video_check --log-dir logs --cases P01 --format markdown`。
- [ ] full suite 完成后运行 `python -m backend.mobile_video_check --log-dir logs --format markdown`。
- [ ] 如果报告为 `mobile_video_needs_transcode`，记录转码命令、转码后文件大小和手机播放结果。

## 23. 模型权重完整性与体积审计

目标：GPU 主机下载权重后，不能只检查目录存在；必须确认 P01 和 full suite 需要的 checkpoint 组没有明显缺失或下载不完整。

- [x] 新增 `backend/model_audit.py`。
- [x] 支持 `p01` profile：`base`、`turbo_vae`、`t5gemma`、`stable_audio`、`wan_vae`，预期约 114.64 GiB。
- [x] 支持 `required_suite` profile：P01 + `540p_sr` + `1080p_sr`，预期约 228.62 GiB。
- [x] 支持 `complete` profile：完整 daVinci-MagiHuman 栈 + 外部模型，预期约 285.61 GiB。
- [x] 支持 `--strict`，模型组缺失或体积明显不足时返回非零状态。
- [x] `scripts/run_p01_smoke_pipeline.sh` 已集成 P01 model audit。
- [x] `scripts/gpu_reproduction_pipeline.sh` 已集成 required-suite model audit。
- [x] 本地测试通过：`python -m unittest tests.test_model_audit -v`。
- [x] 当前本机审计结果为 `not_ready`，P01 需要约 114.64 GiB，实际 0.0000 GiB；这是因为本机不下载模型权重。
- [ ] GPU 主机下载后运行 `python -m backend.model_audit --model-root models --profile p01 --format markdown`。
- [ ] full suite 前运行 `python -m backend.model_audit --model-root models --profile required_suite --format markdown`。
- [ ] 如果审计结果是 `missing` 或 `too_small`，先重新下载对应模型组，不进入推理。

## 24. GPU 证据包 Manifest 与导入审计补齐

目标：GPU 主机跑完后，要把小证据文件完整带回本地，但不能夹带视频、模型权重或第三方源码。

- [x] 新增 `backend/evidence_package.py`。
- [x] `scripts/package_gpu_evidence.sh` 已纳入 `logs/*preflight*.json` 和 `logs/*model_audit*.json`。
- [x] `scripts/package_gpu_evidence.sh` 已纳入 `logs/*hf_access*.json` 和 `logs/*artifact_audit*.json`。
- [x] 证据包会生成 `evidence-manifest.json` 和 `evidence-manifest.md`。
- [x] 证据包会生成 `evidence-provenance.json` 和 `evidence-provenance.md`，记录项目 commit、官方源码 commit、dirty 状态和 P01 manifest hash。
- [x] 证据包会包含 `docs/p01-smoke-manifest.json` 和 `docs/p01-smoke-manifest.md`。
- [x] manifest 会拒绝 `.mp4`、`.mov`、`.mkv`、`.safetensors`、`.ckpt`、`.pt`、`.pth`、`.onnx` 等大文件/模型文件。
- [x] `backend/evidence_import.py` 已把 mobile video 缺证据列入 Missing Evidence。
- [x] 当前 `docs/gpu-evidence-import-audit.md` 已显示 `mobile_video: P01, P03, P04, T01, T02` 缺失。
- [x] 本地测试通过：`python -m unittest tests.test_evidence_package tests.test_evidence_provenance tests.test_evidence_import -v`。
- [x] 本地临时打包验证通过：manifest 状态为 `ok`，没有 forbidden files。
- [ ] GPU 主机跑完后执行 `bash scripts/package_gpu_evidence.sh`。
- [ ] 检查证据包内的 `logs/*artifact_audit*.json`，确认 pipeline 小证据文件齐全。
- [ ] 检查证据包内的 `evidence-manifest.md`，确认没有视频和模型权重。
- [ ] 检查证据包内的 `evidence-provenance.md`，确认项目 commit、官方源码 commit 和 P01 manifest hash 可追溯。
- [ ] 把证据包解压回本项目后重新运行 `python -m backend.evidence_import --log-dir logs --quality-review docs/quality-review.json --cost-review docs/cost-review.json --final-report-output docs/mobile-feasibility-report.md --format markdown --output docs/gpu-evidence-import-audit.md`。

## 25. 模型审计 strict 顺序修正

目标：新 GPU 主机第一次运行 `DOWNLOAD_MODELS=1` 时，不能在模型下载之前就因为 strict model audit 失败而退出。

- [x] 修正 `scripts/run_p01_smoke_pipeline.sh`：下载前 model audit 只报告，下载后 model audit 才 strict。
- [x] 修正 `scripts/gpu_reproduction_pipeline.sh`：full suite 同样采用下载前报告、下载后 strict 的顺序。
- [x] 保留直接执行场景的 strict 行为：当 `EXECUTE=1` 且 `DOWNLOAD_MODELS!=1` 时，模型应已存在，初始 model audit 必须 strict。
- [x] 新增 `tests/test_pipeline_scripts.py`，覆盖 fresh GPU host 下载场景。
- [x] 本地 dry-run 通过：模型缺失会报告 `not_ready`，但 `EXECUTE=0` 不阻断。
- [ ] 在 GPU 主机上用 `DOWNLOAD_MODELS=1 EXECUTE=1` 实测该顺序：应先下载，再执行 strict post-download audit。

## 26. Checkpoint 下载 Profile

目标：P01 首次复现不应先下载完整 285 GiB 权重；应先只下载 P01 必需权重，P01 通过后再补齐 full suite 所需 SR 权重。

- [x] `scripts/download_models.sh` 新增 `MODEL_PROFILE`。
- [x] `MODEL_PROFILE=p01` 只下载 `base/*`、`turbo_vae/*` 和外部模型。
- [x] `MODEL_PROFILE=required_suite` 下载 `base/*`、`turbo_vae/*`、`540p_sr/*`、`1080p_sr/*` 和外部模型。
- [x] `MODEL_PROFILE=complete` 保持完整下载，包括 distill。
- [x] `scripts/download_models.sh` 新增 `DRY_RUN=1`，可在本机预览下载命令而不触发真实下载。
- [x] `scripts/download_models.sh` 会在真实下载和 dry-run 时打印 `download_command=...`，便于审计实际 Hugging Face 命令。
- [x] P01 pipeline 默认使用 `MODEL_PROFILE=p01`。
- [x] full GPU pipeline 默认使用 `MODEL_PROFILE=required_suite`。
- [x] 新增 `backend/download_log_audit.py`，自动检查 P01 下载日志不包含 `540p_sr/*`、`1080p_sr/*`、`distill/*`。
- [x] P01/full pipeline 在模型下载后自动运行 download log audit。
- [x] 本地测试通过：`python -m unittest tests.test_download_models_script -v`。
- [x] 本地 download log audit 测试通过：`python -m unittest tests.test_download_log_audit -v`。
- [ ] GPU 主机首次 P01 运行确认下载日志里只包含 `base/*` 和 `turbo_vae/*`，不包含 `540p_sr/*`、`1080p_sr/*`、`distill/*`。
- [ ] P01 成功后再运行 `MODEL_PROFILE=required_suite bash scripts/download_models.sh` 补齐 SR 权重。

## 27. P01 Smoke 输入 Manifest

目标：GPU 首次 P01 复现前，先把实际执行输入固定下来，避免后续 metrics、视频、质量评审无法追溯到具体 prompt、seed、时长和官方源码版本。

- [x] `backend/experiment_matrix.py` 为 P01/P03/P04/T01/T02 等 case 显式设置 `seed=42`。
- [x] `scripts/magihuman_task_runner.sh` 传入 `--seed "${MAGIHUMAN_SEED:-42}"`。
- [x] 新增 `backend/smoke_manifest.py`，生成 P01 输入 manifest。
- [x] 生成 `docs/p01-smoke-manifest.json` 和 `docs/p01-smoke-manifest.md`。
- [x] manifest 记录官方源码 commit、P01 prompt hash、seed、5 秒时长、256p/448x256、预期输出 `outputs/smoke-test/P01.mp4`。
- [x] manifest 记录官方 `example/base/run_T2V.sh` 的 4 秒默认示例参数，用于区分官方示例和本项目 P01 实测目标。
- [x] manifest 记录官方 `example/assets/image.png` hash；P01 为 T2V 不使用该图，后续 TI2V 使用。
- [x] 新增 `backend/smoke_plan_audit.py`，自动检查 `docs/p01-smoke-manifest.json` 与当前 P01 pipeline 计划一致。
- [x] 生成 `docs/p01-smoke-plan-audit.json` 和 `docs/p01-smoke-plan-audit.md`，当前状态为 `ready`。
- [x] `scripts/run_p01_smoke_pipeline.sh` 会在下载/执行前运行 `backend.smoke_plan_audit --strict`。
- [x] 本地测试通过：`python -m unittest tests.test_smoke_manifest tests.test_experiment_matrix tests.test_experiment_runner -v`。
- [x] 本地 smoke plan audit 测试通过：`python -m unittest tests.test_smoke_plan_audit -v`。
- [ ] GPU 主机跑 P01 前检查 `docs/p01-smoke-manifest.json` 与 `outputs/reports/p01_smoke_plan_*.sh` 一致。
- [ ] P01 跑完后把 `logs/*P01*_metrics.json` 与 manifest 中的 seed、duration、resolution、result path 对齐检查。

## 28. Metrics 输入上下文

目标：GPU 跑完后，`logs/*_metrics.json` 自身必须能说明它对应哪个实验 case、哪个 seed、哪个目标时长/分辨率和哪个 manifest，不能只靠文件名推断。

- [x] `backend/run_metrics.py` 支持可选 `run` 上下文字段。
- [x] `run` 字段包含 case id、mode、resolution、variant、seed、目标时长、目标 base/SR 尺寸、result path。
- [x] `run` 字段记录 prompt SHA-256，而不是只依赖 shell 日志里的原始 prompt。
- [x] `run` 字段支持 manifest path 和 manifest SHA-256。
- [x] `scripts/magihuman_task_runner.sh` 自动把这些上下文传给 `backend.run_metrics`。
- [x] P01 的 runner env 自动带上 `MAGIHUMAN_MANIFEST_PATH=docs/p01-smoke-manifest.json`。
- [x] 本地测试通过：`python -m unittest tests.test_run_metrics tests.test_experiment_matrix tests.test_smoke_manifest -v`。
- [ ] GPU 主机跑完 P01 后检查 `logs/*P01*_metrics.json` 中 `run.case_id=P01`、`run.seed=42`、`run.target_duration_seconds=5.0`。
- [ ] 检查 P01 metrics 中 `run.manifest_sha256` 与 `docs/p01-smoke-manifest.json` 当前 hash 一致。

## 29. Metrics Context Audit

目标：GPU 证据导入时自动检查 metrics 的 `run` 上下文，避免把跑错 seed、跑错输入、缺少 manifest 追踪的 metrics 用进最终手机 App 判断。

- [x] 新增 `backend/metrics_context_audit.py`。
- [x] 审计 `logs/*_metrics.json` 是否包含 `run.case_id`、`run.mode`、`run.resolution`、`run.variant`、`run.seed`、目标时长/分辨率和 prompt hash。
- [x] 对 P01 额外校验 `docs/p01-smoke-manifest.json` 的 manifest path、manifest hash、seed、时长、base 分辨率、prompt hash 和预期输出路径。
- [x] `backend/evidence_import.py` 已集成 metrics context audit；metrics 缺上下文或上下文不匹配时，最终报告不能进入 `ready_for_final_update`。
- [x] `docs/gpu-evidence-import-audit.md` 已新增 `Metrics Context Audit` 段落。
- [x] 当前本机 metrics context 状态为 `missing_metrics`，原因是还没有导入真实 GPU metrics。
- [x] 本地测试通过：`python -m unittest tests.test_metrics_context_audit tests.test_evidence_import tests.test_feasibility_decision -v`。
- [ ] GPU 主机证据包导入后运行 `python -m backend.metrics_context_audit --log-dir logs --format markdown`。
- [ ] P01 导入后确认 context audit 中 P01 状态为 `context_ready`。
- [ ] full suite 导入后确认 P01/P03/P04/T01/T02 的 metrics context 都没有 `missing_run_context` 或 `context_mismatch`。

## 30. Evidence Package Provenance

目标：GPU 证据包必须说明它来自哪个项目 commit、哪个官方源码 commit，以及 P01 manifest 的 hash，避免把来源不明的 metrics 用进最终判断。

- [x] 新增 `backend/evidence_provenance.py`。
- [x] provenance 记录项目 Git commit、branch、dirty 状态和 dirty paths。
- [x] provenance 记录 `third_party/daVinci-MagiHuman` commit，并与锁定 commit `209209b7086eba2020c5439265221495a8357322` 对比。
- [x] provenance 记录 `third_party/MagiCompiler` commit，并与锁定 commit `bfef5bc70226a0c0740e4c551e4f7245a974fb4f` 对比。
- [x] provenance 记录 `docs/p01-smoke-manifest.json` 的 SHA-256。
- [x] `scripts/package_gpu_evidence.sh` 自动生成 `evidence-provenance.json` 和 `evidence-provenance.md`。
- [x] `scripts/package_gpu_evidence.sh` 自动把 P01 manifest JSON/Markdown 纳入证据包。
- [x] 本地临时打包验证通过：provenance 文件和 P01 manifest 文件都进入证据包，manifest 状态为 `ok`。
- [x] 本地测试通过：`python -m unittest tests.test_evidence_provenance tests.test_evidence_package -v`。
- [ ] GPU 证据包返回后检查 `evidence-provenance.md` 中官方源码 commit 是否匹配锁定值。
- [ ] GPU 证据包返回后检查 provenance 中 P01 manifest SHA-256 是否与 metrics context audit 使用的 manifest 一致。

## 31. Hugging Face 权限 Probe 审计

目标：云 GPU 第一次下载 100+ GiB 权重前，先确认 Hugging Face token 能访问具体仓库和代表性权重文件，尤其是 gated 的 `google/t5gemma-9b-9b-ul2` 与 `stabilityai/stable-audio-open-1.0`，避免在大下载中途才发现未接受条款或 token 无权限。

- [x] 新增 `backend/hf_access_audit.py`。
- [x] token 来源支持命令参数、`HF_TOKEN`、`HUGGINGFACE_HUB_TOKEN` 和 `~/.cache/huggingface/token`。
- [x] `p01` profile 检查：
  - `GAIR/daVinci-MagiHuman/base/model-00003-of-00007.safetensors`
  - `GAIR/daVinci-MagiHuman/turbo_vae/checkpoint-340000.ckpt`
  - `google/t5gemma-9b-9b-ul2/model-00007-of-00009.safetensors`
  - `stabilityai/stable-audio-open-1.0/model.ckpt`
  - `Wan-AI/Wan2.2-TI2V-5B/models_t5_umt5-xxl-enc-bf16.pth`
- [x] `required_suite` profile 在 P01 基础上额外检查：
  - `GAIR/daVinci-MagiHuman/540p_sr/model-00013-of-00013.safetensors`
  - `GAIR/daVinci-MagiHuman/1080p_sr/model-00006-of-00013.safetensors`
- [x] `complete` profile 额外检查：
  - `GAIR/daVinci-MagiHuman/distill/model-00012-of-00013.safetensors`
- [x] 输出 JSON 和 Markdown：`logs/*hf_access*.json`、`outputs/reports/*hf_access*.md`。
- [x] `scripts/package_gpu_evidence.sh` 会把 `logs/*hf_access*.json` 纳入小证据包；Markdown 报告会随 `outputs/reports/*.md` 一起纳入。
- [x] `--strict` 模式下任一 probe 失败会返回非零状态，阻止继续下载。
- [x] P01 pipeline 在下载前自动执行 `backend.hf_access_audit --profile p01 --strict`。
- [x] full pipeline 在下载前自动执行 `backend.hf_access_audit --profile required_suite --strict`。
- [x] 新增 `tests/test_hf_access_audit.py`，覆盖 token 解析、缺 token 的 gated probe、403 权限失败、URL 编码和 Markdown 输出。
- [x] 本地验证通过：`python -m unittest tests.test_hf_access_audit tests.test_pipeline_scripts tests.test_p01_smoke_pipeline -v`，11 个 targeted tests 通过。
- [x] Bash 语法验证通过：`bash -n scripts/run_p01_smoke_pipeline.sh` 和 `bash -n scripts/gpu_reproduction_pipeline.sh`。
- [ ] GPU 主机设置 token：`export HF_TOKEN=<token>`。
- [ ] GPU 主机运行 `python -m backend.hf_access_audit --profile p01 --format markdown`，保存输出。
- [ ] 如果 `google/t5gemma-9b-9b-ul2` 是 `auth_required_or_forbidden`，先到 Hugging Face 接受该模型条款，再重跑 audit。
- [ ] 如果 `stabilityai/stable-audio-open-1.0` 是 `auth_required_or_forbidden`，先到 Hugging Face 接受该模型条款，再重跑 audit。
- [ ] P01 access audit 全部 `accessible` 后再运行 `INSTALL_MAGICOMPILER=1 DOWNLOAD_MODELS=1 EXECUTE=1 bash scripts/run_p01_smoke_pipeline.sh`。
- [ ] P01 成功后运行 `python -m backend.hf_access_audit --profile required_suite --format markdown`，确认 SR probe 也可访问。
- [ ] required-suite access audit 全部 `accessible` 后再运行 `EXECUTE=1 bash scripts/gpu_reproduction_pipeline.sh` 或 `DOWNLOAD_MODELS=1 EXECUTE=1 bash scripts/gpu_reproduction_pipeline.sh`。
- [ ] 证据包返回后检查 `outputs/reports/p01_hf_access_*.md` 和 `outputs/reports/hf_access_*.md`，把结果同步进 `docs/reproduction-log.md`。

## 32. Pipeline Artifact Audit

目标：GPU pipeline 跑完后，自动检查这次运行的关键小证据是否齐全，包括 preflight、HF access、model audit、下载日志、执行日志、metrics、结果 mp4、汇总报告、移动端视频报告、最终报告。这样在打包证据前就能发现漏文件，而不是导入回本地后才发现证据链断了。

- [x] 新增 `backend/pipeline_artifact_audit.py`。
- [x] 支持 `--run p01`，按 pipeline timestamp 检查 P01 的：
  - `logs/p01_preflight_<timestamp>.json`
  - `outputs/reports/p01_preflight_<timestamp>.md`
  - `logs/p01_model_audit_<timestamp>.json`
  - `outputs/reports/p01_model_audit_<timestamp>.md`
  - `logs/p01_smoke_plan_audit_<timestamp>.json`
  - `outputs/reports/p01_smoke_plan_audit_<timestamp>.md`
  - `outputs/reports/p01_smoke_plan_<timestamp>.sh`
  - 可选 `logs/p01_hf_access_<timestamp>.json` / `outputs/reports/p01_hf_access_<timestamp>.md`
  - 可选 `logs/p01_download_models_<timestamp>.log`
  - 可选 `logs/p01_download_log_audit_<timestamp>.json` / `outputs/reports/p01_download_log_audit_<timestamp>.md`
  - 可选下载后 preflight/model audit
  - `EXECUTE=1` 时检查 `logs/p01_smoke_execute_<timestamp>.log`、`logs/P01_*_metrics.json`、`outputs/smoke-test/P01.mp4`
  - `EXECUTE=1` 时检查 `logs/p01_acceptance_<timestamp>.json`、`outputs/reports/p01_acceptance_<timestamp>.md`
  - P01 experiment summary、mobile video、feasibility decision、final report。
- [x] 支持 `--run full`，按 pipeline timestamp 检查 full suite 的：
  - `logs/gpu_preflight_<timestamp>.json`
  - `outputs/reports/gpu_preflight_<timestamp>.md`
  - `logs/model_audit_<timestamp>.json`
  - `outputs/reports/model_audit_<timestamp>.md`
  - 可选 `logs/hf_access_<timestamp>.json` / `outputs/reports/hf_access_<timestamp>.md`
  - 可选 `logs/download_models_<timestamp>.log`
  - 可选 `logs/download_log_audit_<timestamp>.json` / `outputs/reports/download_log_audit_<timestamp>.md`
  - `EXECUTE=0` 时检查 `logs/experiment_suite_dryrun_<timestamp>.sh`
  - `EXECUTE=1` 时检查 `logs/experiment_suite_<timestamp>.log`、P01/P03/P04/T01/T02 metrics、P01/P03/P04/T01/T02 mp4
  - full experiment summary、mobile video、feasibility decision、final report。
- [x] `scripts/run_p01_smoke_pipeline.sh` 结束时自动生成：
  - `logs/p01_pipeline_artifact_audit_<timestamp>.json`
  - `outputs/reports/p01_pipeline_artifact_audit_<timestamp>.md`
- [x] `scripts/gpu_reproduction_pipeline.sh` 结束时自动生成：
  - `logs/pipeline_artifact_audit_<timestamp>.json`
  - `outputs/reports/pipeline_artifact_audit_<timestamp>.md`
- [x] `scripts/package_gpu_evidence.sh` 会把 `logs/*artifact_audit*.json` 纳入证据包。
- [x] 新增 `tests/test_pipeline_artifact_audit.py`，覆盖 P01 dry-run、P01 下载+执行、full dry-run、缺失 artifact、脚本集成。
- [x] 本地验证通过：`python -m unittest tests.test_pipeline_artifact_audit tests.test_pipeline_scripts tests.test_p01_smoke_pipeline tests.test_evidence_package -v`，14 个 targeted tests 通过。
- [x] Bash 语法验证通过：`bash -n scripts/run_p01_smoke_pipeline.sh`、`bash -n scripts/gpu_reproduction_pipeline.sh`、`bash -n scripts/package_gpu_evidence.sh`。
- [ ] GPU 主机 P01 成功后检查 `outputs/reports/p01_pipeline_artifact_audit_*.md`，确认状态为 `ready`。
- [ ] GPU 主机 full suite 成功后检查 `outputs/reports/pipeline_artifact_audit_*.md`，确认状态为 `ready`。
- [ ] 如果 artifact audit 显示缺少 metrics 或 mp4，不进入质量评审和最终 App 判断；先重跑对应 case。
- [ ] 证据包返回本地后，把 artifact audit 结论同步进 `docs/reproduction-log.md`。

## 33. Upstream Drift Audit

目标：真正使用云 GPU 跑复现前，确认官方 GitHub/Hugging Face 上游元数据没有相对本项目锁定值发生漂移。如果上游移动了，需要明确是继续复现锁定版本，还是更新锁定值并重新做静态核验。

- [x] 新增 `backend/upstream_drift_audit.py`。
- [x] 检查 daVinci-MagiHuman GitHub main：
  - locked：`209209b7086eba2020c5439265221495a8357322`
  - upstream live：`209209b7086eba2020c5439265221495a8357322`
  - status：`matches_lock`
- [x] 检查 MagiCompiler GitHub main：
  - locked：`bfef5bc70226a0c0740e4c551e4f7245a974fb4f`
  - upstream live：`bfef5bc70226a0c0740e4c551e4f7245a974fb4f`
  - status：`matches_lock`
- [x] 检查 Hugging Face model `GAIR/daVinci-MagiHuman`：
  - locked：`7fe95e50c11bd92bdadf94cd51dbec18427f8e0c`
  - upstream live：`7fe95e50c11bd92bdadf94cd51dbec18427f8e0c`
  - last modified：`2026-03-25T14:00:27.000Z`
  - status：`matches_lock`
- [x] 检查 Hugging Face Space `SII-GAIR/daVinci-MagiHuman`：
  - locked：`f4ca1ddf0ab78843686894301a8d0d7ec2cf027b`
  - upstream live：`f4ca1ddf0ab78843686894301a8d0d7ec2cf027b`
  - last modified：`2026-03-24T12:36:17.000Z`
  - status：`matches_lock`
- [x] 生成 `docs/upstream-drift-audit.json` 和 `docs/upstream-drift-audit.md`。
- [x] 当前 live audit 总状态为 `locked_current`。
- [x] `scripts/bootstrap_gpu_host.sh` 会默认生成 `outputs/reports/upstream_drift_audit.md/json`。
- [x] 支持 `--strict`，当 upstream moved 或 unreachable 时返回非零状态。
- [x] 新增 `tests/test_upstream_drift_audit.py`，覆盖 lock 匹配、漂移检测、不可达、Markdown 输出和 bootstrap 集成。
- [x] 本地验证通过：`python -m unittest tests.test_upstream_drift_audit -v`。
- [ ] GPU 主机 bootstrap 后检查 `outputs/reports/upstream_drift_audit.md`。
- [ ] 如果状态是 `drift_detected`，先不要启动下载/推理；更新 `docs/reproduction-log.md` 说明继续锁定版本或升级锁定版本的决定。
- [ ] 如果状态是 `incomplete`，记录网络/权限问题，并用本地 `docs/upstream-drift-audit.md` 作为临时依据。

## 34. P01 Smoke Plan Audit

目标：P01 首次 GPU 执行前，必须确认 `docs/p01-smoke-manifest.json` 与当前自动生成的 P01 执行计划完全一致，避免后续 metrics 指向的 prompt、seed、duration、result path 和 manifest path 与文档记录不一致。

- [x] 新增 `backend/smoke_plan_audit.py`。
- [x] 审计字段包括 manifest type、case id、case name、mode、resolution、variant、duration、seed、prompt SHA-256、base/SR 尺寸、runner command、expected result path。
- [x] 审计 `MAGIHUMAN_TASK_ID`、`MAGIHUMAN_PROMPT`、`MAGIHUMAN_MODE`、`MAGIHUMAN_RESOLUTION`、`MAGIHUMAN_DURATION_SECONDS`、`MAGIHUMAN_SEED`、`MAGIHUMAN_MODEL_VARIANT`、`MAGIHUMAN_RESULT_PATH`、`MAGIHUMAN_MANIFEST_PATH`。
- [x] 生成 `docs/p01-smoke-plan-audit.json` 和 `docs/p01-smoke-plan-audit.md`。
- [x] 当前审计状态为 `ready`，P01 prompt hash、seed=42、duration=5、result path=`outputs/smoke-test/P01.mp4` 均匹配。
- [x] P01 pipeline 在初始 preflight/model audit 后、HF access/download/execute 前自动运行 smoke plan audit。
- [x] P01 pipeline 输出：
  - `logs/p01_smoke_plan_audit_<timestamp>.json`
  - `outputs/reports/p01_smoke_plan_audit_<timestamp>.md`
- [x] `backend.pipeline_artifact_audit` 已把 P01 smoke plan audit 纳入 P01 artifact 检查。
- [x] `scripts/package_gpu_evidence.sh` 会把 `logs/*smoke_plan_audit*.json` 纳入证据包。
- [x] 新增 `tests/test_smoke_plan_audit.py`，覆盖默认 ready、result-dir mismatch、seed mismatch、Markdown 输出和脚本集成。
- [x] 本地验证通过：`python -m unittest tests.test_smoke_plan_audit tests.test_pipeline_artifact_audit tests.test_p01_smoke_pipeline tests.test_evidence_package -v`，16 个 targeted tests 通过。
- [ ] GPU 主机运行 P01 前检查 `outputs/reports/p01_smoke_plan_audit_*.md` 状态为 `ready`。
- [ ] 如果 smoke plan audit 不是 `ready`，不要下载模型或执行推理；先修正 manifest 或 experiment matrix。
- [ ] P01 证据包返回本地后，把 smoke plan audit 结论同步进 `docs/reproduction-log.md`。

## 35. Checkpoint Download Log Audit

目标：P01 首次 GPU 复现不能误下载完整主模型栈。下载日志必须能证明 P01 只请求 `base/*` 和 `turbo_vae/*`，required-suite 只请求 base/turbo/SR，不请求 distill。

- [x] 修改 `scripts/download_models.sh`：每个 `huggingface-cli download ...` 执行前都会打印 `download_command=...`。
- [x] 新增 `backend/download_log_audit.py`。
- [x] `p01` profile 要求：
  - `GAIR/daVinci-MagiHuman --include base/*`
  - `GAIR/daVinci-MagiHuman --include turbo_vae/*`
  - `google/t5gemma-9b-9b-ul2`
  - `stabilityai/stable-audio-open-1.0`
  - `Wan-AI/Wan2.2-TI2V-5B`
- [x] `p01` profile 禁止：
  - `540p_sr/*`
  - `1080p_sr/*`
  - `distill/*`
- [x] `required_suite` profile 要求 base、turbo、`540p_sr/*`、`1080p_sr/*` 和三个外部仓库。
- [x] `required_suite` profile 禁止 `distill/*`。
- [x] `complete` profile 要求主仓库命令不带 `--include`，用于完整镜像。
- [x] P01 pipeline 在 `scripts/download_models.sh` 后自动生成：
  - `logs/p01_download_log_audit_<timestamp>.json`
  - `outputs/reports/p01_download_log_audit_<timestamp>.md`
- [x] full pipeline 在 `scripts/download_models.sh` 后自动生成：
  - `logs/download_log_audit_<timestamp>.json`
  - `outputs/reports/download_log_audit_<timestamp>.md`
- [x] `backend.pipeline_artifact_audit` 已把 download log audit 纳入 `DOWNLOAD_MODELS=1` 的 artifact 检查。
- [x] `scripts/package_gpu_evidence.sh` 会把 `logs/*download_log_audit*.json` 纳入证据包。
- [x] 新增 `tests/test_download_log_audit.py`，覆盖 P01 ready、P01 错误包含 SR、required-suite ready、外部仓库缺失和 Markdown 输出。
- [x] 本地验证通过：`python -m unittest tests.test_download_log_audit tests.test_download_models_script tests.test_pipeline_scripts tests.test_pipeline_artifact_audit tests.test_evidence_package -v`，21 个 targeted tests 通过。
- [ ] GPU 主机 P01 下载后检查 `outputs/reports/p01_download_log_audit_*.md` 状态为 `ready`。
- [ ] 如果 P01 download log audit 检测到 `540p_sr/*`、`1080p_sr/*` 或 `distill/*`，不要继续 P01 推理；先修正下载 profile。
- [ ] full suite 下载后检查 `outputs/reports/download_log_audit_*.md` 状态为 `ready`。

## 36. P01 Smoke Acceptance Gate

目标：P01 跑完后不能只凭“有一个 mp4 文件”就进入 full suite。必须用可审计证据确认 P01 的运行数据、输入上下文、视频时长、音视频流和手机播放兼容性都达到最低要求。

- [x] 新增 `backend/p01_acceptance.py`。
- [x] 检查 `backend.experiment_results` 中 P01 状态必须为 `measured`。
- [x] 检查 `backend.metrics_context_audit` 中 P01 状态必须为 `context_ready`，确保 metrics 的 seed、prompt hash、duration、resolution、manifest hash 与 `docs/p01-smoke-manifest.json` 对齐。
- [x] 检查 `outputs/smoke-test/P01.mp4` 是否存在。
- [x] 检查 P01 视频时长接近 5 秒，默认容忍范围为 +/- 1 秒。
- [x] 检查 P01 metrics 中 `has_audio=True` 且 `has_video=True`。
- [x] 检查手机视频证据必须为 `mobile_video_ready` 或 `mobile_video_needs_transcode`。
- [x] 状态定义：
  - `not_ready`：不进入 P03/P04/T01/T02，先修复失败项。
  - `ready_for_full_suite`：可以进入 full suite。
  - `ready_for_full_suite_with_transcode_required`：可以进入 full suite，但最终 App 可行性必须补齐移动端转码/交付路径。
- [x] `scripts/run_p01_smoke_pipeline.sh` 在 `EXECUTE=1` 后自动生成：
  - `logs/p01_acceptance_<timestamp>.json`
  - `outputs/reports/p01_acceptance_<timestamp>.md`
- [x] `backend.pipeline_artifact_audit` 已把 P01 acceptance JSON/Markdown 纳入 `EXECUTE=1` 的 P01 artifact 检查。
- [x] `scripts/package_gpu_evidence.sh` 会把 `logs/*p01_acceptance*.json` 纳入证据包。
- [x] 新增 `tests/test_p01_acceptance.py`，覆盖缺 metrics、证据 ready、需要转码但可继续、时长不合格和 Markdown 输出。
- [x] 本地 targeted 验证通过：`python -m unittest tests.test_p01_acceptance tests.test_pipeline_artifact_audit tests.test_p01_smoke_pipeline tests.test_evidence_package -v`，16 个 targeted tests 通过。
- [x] Bash 语法验证通过：`bash -n scripts/run_p01_smoke_pipeline.sh`、`bash -n scripts/package_gpu_evidence.sh`。
- [ ] GPU 主机 P01 跑完后检查 `outputs/reports/p01_acceptance_*.md`。
- [ ] 如果状态为 `not_ready`，不要运行 full suite；先按 failed checks 修复并重跑 P01。
- [ ] 如果状态为 `ready_for_full_suite_with_transcode_required`，允许继续 full suite，但在最终 App 方案中记录 ffmpeg 转码或服务端转码策略。
- [ ] 只有当状态以 `ready_for_full_suite` 开头时，才运行 `EXECUTE=1 bash scripts/gpu_reproduction_pipeline.sh`。

## 37. Required Suite Acceptance Gate

目标：full suite 跑完后，不能只凭 metrics 文件存在就进入人工质量评审、成本评审和最终 App 判断。必须确认 P01/P03/P04/T01/T02 的运行数据、输入上下文、结果 mp4、时长、音视频流和手机播放证据都齐全。

- [x] 新增 `backend/required_suite_acceptance.py`。
- [x] 默认检查 required case：`P01`、`P03`、`P04`、`T01`、`T02`。
- [x] 每个 case 检查 `backend.experiment_results` 状态必须为 `measured`。
- [x] 每个 case 检查 `backend.metrics_context_audit` 状态必须为 `context_ready`。
- [x] 每个 case 检查结果 mp4 存在；P01 默认复用 `outputs/smoke-test/P01.mp4`，其他 case 使用 `outputs/experiment-results/<case>.mp4`。
- [x] 每个 case 检查视频时长接近目标 5 秒，默认容忍范围为 +/- 1 秒。
- [x] 每个 case 检查 `has_audio=True` 且 `has_video=True`。
- [x] 每个 case 检查手机视频证据必须为 `mobile_video_ready` 或 `mobile_video_needs_transcode`。
- [x] 状态定义：
  - `not_ready`：不进入质量评审、成本评审或最终 App 判断。
  - `ready_for_quality_and_cost_review`：可以创建并填写质量评审和成本评审。
  - `ready_for_quality_and_cost_review_with_transcode_required`：可以继续评审，但最终 App 方案必须包含移动端转码/交付路径。
- [x] `scripts/gpu_reproduction_pipeline.sh` 在 `EXECUTE=1` 后自动生成：
  - `logs/required_suite_acceptance_<timestamp>.json`
  - `outputs/reports/required_suite_acceptance_<timestamp>.md`
- [x] `backend.pipeline_artifact_audit` 已把 required-suite acceptance JSON/Markdown 纳入 full execute artifact 检查。
- [x] full artifact audit 接受 P01 结果来自 `outputs/smoke-test/P01.mp4`，避免 full suite 复用 P01 smoke evidence 时误报缺文件。
- [x] `scripts/package_gpu_evidence.sh` 会把 `logs/*required_suite_acceptance*.json` 纳入证据包。
- [x] 新增 `tests/test_required_suite_acceptance.py`，覆盖缺 metrics、全量 ready、需要转码但可继续、缺结果 mp4、context mismatch。
- [x] 本地 targeted 验证通过：`python -m unittest tests.test_required_suite_acceptance tests.test_pipeline_artifact_audit tests.test_pipeline_scripts tests.test_evidence_package -v`，18 个 targeted tests 通过。
- [x] Bash 语法验证通过：`bash -n scripts/gpu_reproduction_pipeline.sh`、`bash -n scripts/package_gpu_evidence.sh`。
- [ ] GPU 主机 full suite 跑完后检查 `outputs/reports/required_suite_acceptance_*.md`。
- [ ] 如果状态为 `not_ready`，不要创建质量/成本评审；先按 failed checks 修复并重跑对应 case。
- [ ] 如果状态为 `ready_for_quality_and_cost_review_with_transcode_required`，继续评审，但在最终 App 可行性里记录转码策略和成本。
- [ ] 只有当状态以 `ready_for_quality_and_cost_review` 开头时，才进入 `docs/quality-review.json` 和 `docs/cost-review.json`。
